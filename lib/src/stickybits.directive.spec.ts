import { StickybitsDirective } from './stickybits.directive';

describe('StickybitsDirective', () => {
  it('should create an instance', () => {
    const directive = new StickybitsDirective();
    expect(directive).toBeTruthy();
  });
});
